# AShareAuto · A股自动交易系统

> 与 [PolyAuto](README.md) 同一套设计哲学的多策略 A股 自动交易系统。
> **A股系统是主界面(port 5000),Polymarket 机器人降为子界面(头部链接按钮跳转 5001)。**
> 保留实盘接口(QMT),默认**模拟盘**运行。

## ⚠️ 诚实风险声明(务必阅读)

**A股没有稳赚策略。** 本系统不做任何不切实际的盈利承诺。
"高胜率"来自:**确定性 edge(可转债双低/打新)+ 严格多因子过滤 + 真实数据回测验证 + 纪律化风控**,
它提升的是**决策质量与回撤控制**,不是"稳赚"。请只用你能承受损失的金额参与。
任何回测结果都包含幸存者偏差等系统性误差,仅作参考,不代表未来表现。

---

## 架构

| 文件 | 职责 |
|------|------|
| `ashare_trader.py` | 主程序 · Flask 主界面(5000)· 会话鉴权 · 调度线程 |
| `ashare_strategy_engine.py` | 4 大策略 + 评分 + 仓位 + 分层风控 + 表现追踪 |
| `ashare_broker.py` | 券商适配层 · PaperBroker(仿真)/ QMTBroker(实盘,可选) |
| `ashare_data.py` | akshare 数据封装 + SQLite 缓存 + 断线重试/熔断 |
| `ashare_backtest.py` | 回测引擎 + 高胜率门禁 + 报告 JSON |
| `run_ashare.bat` | Windows 一键启动 |
| `requirements-ashare.txt` | Python 依赖 |

数据/状态/配置均存于 `data/`(已 gitignore,不含任何敏感信息)。

## 快速开始

```bash
# 1. 安装依赖
pip install -r requirements-ashare.txt

# 2. 启动(Windows 可用 run_ashare.bat)
python ashare_trader.py

# 3. 浏览器打开 http://localhost:5000
#    点「授权并启动」→ 默认以模拟盘运行(本金 ¥100,000)
```

> PolyAuto(5001)需单独启动:`python auto_trader.py`。
> 本系统顶部「Polymarket 机器人 ↗」按钮可一键跳转。

## 四大策略(稳健优先)

1. **可转债双低轮动(核心)** — 双低值 = 债价 + 转股溢价率点数。
   过滤债价≤115、溢价率≤40%、规模≥2亿、评级≥AA、排除 ST 正股/临期。
   每周调仓等权。"跌有债底、涨跟正股" → 高胜率小回撤。
2. **可转债打新** — 免费顶格申购提醒,中签上市首日卖出纪律。无本金占用。
3. **红利低波** — 沪深300 股息率 top 且 60 日波动率最低,月度调仓等权。
4. **趋势跟随(默认关闭)** — 方向性策略无稳健 edge,仅作实验;MA20/MA60 金叉 + RSI 过滤。

## 分层风控

- 风险滑块 1-10(默认 5):档位越高总敞口越大、现金储备越少
- 单票 ≤5%、总敞口 ≤70%、现金储备 ≥30%
- **回撤 −8% 熔断停开仓**(恢复超一半 DD 才解禁)
- 连亏 3 次冷却、每日交易 ≤10 笔、日亏 −10% 当日硬停
- **交易时段门禁**(仅 9:30-11:30 / 13:00-15:00 下单)+ 股票 T+1 校验
- 可转债 T+0;股票 T+1;涨跌停/手数/费用按真实制度

## 回测报告(界面"🔬 回测报告"页)

`python ashare_backtest.py --strategy cb_double_low --years 3 --bankroll 100000`

回测复用 PaperBroker 撮合规则(费用/T+1/涨跌停/手数),分策略生成:
**总收益 / 年化 / 胜率 / 期望值 / 最大回撤 / 夏普 / 盈亏因子**,并做**高胜率门禁**:

| 策略 | 胜率 | 最大回撤 | 收益 |
|------|------|---------|------|
| 可转债双低 | ≥55% | ≤25% | 必须为正 |
| 红利低波 | ≥55% | ≤20% | 必须为正 |
| 趋势跟随 | — | ≤30% | 必须为正 |

> 门禁失败会**大声报警**,绝不隐藏——这是系统的诚实底线。
> 回测存在幸存者偏差(当前市场成员回放历史)与前视偏差(评级/规模取当前值),仅作策略参考。

## 实盘切换(QMT)

1. 安装 QMT/miniQMT 客户端(极简模式),启用极简交易。
2. 配置 `data/ashare_config.json` 中 `broker="qmt"`、`qmt.path`、`qmt.account_id`。
3. 界面「设置」把交易模式切到 **实盘**,重启授权。
4. **双保险**:仅 `broker=qmt` **且** `trading_mode=live` 才走实盘,其余一律仿真。
   QMT 不可用时自动回退仿真盘,绝不静默走实盘。

> xtquant 不在 PyPI,随 QMT 客户端附带。需将 QMT 的 `bin` 目录加入 `PYTHONPATH`。

## 鉴权模型

- 页面/接口受会话 cookie(`ashareauth`)保护,**清除 cookie 即撤销授权,每个浏览器独立授权**。
- 可在设置中配置 `web_password`,启用 HTTP Basic Auth 双保险。
- 模拟盘模式没有资金风险;实盘模式下请务必设置访问密码并只在可信环境使用。

## 端口约定

| 系统 | 端口 | 入口 |
|------|------|------|
| AShareAuto(A股主界面) | **5000** | http://localhost:5000 |
| Polymarket 机器人(子界面) | **5001** | http://localhost:5001 |

---

AShareAuto · A股自动交易系统 · © 2026 Gavin · 谨慎交易,风险自负 · v1.0.0
